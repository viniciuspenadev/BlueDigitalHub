# Dependências e artefatos de build (reconstruídos dentro da imagem)
node_modules
dist
.astro

# Git e metadados
.git
.gitignore

# Ambiente / segredos (nunca vão para a imagem)
.env
.env.*

# Docker (não precisa dentro do contexto)
Dockerfile
.dockerignore

# Editor / SO
.vscode
.idea
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
*.log
